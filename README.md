# Gitob chocolatey package

This repository packages Gitob for the [chocolatey package manager](https://community.chocolatey.org/packages/gitob) on Windows.

## Installation

```
choco install gitob
```

run from the command line or PowerShell.
